name: Build Beyond Token Time paper

on:
  push:
    branches: [master]
    paths:
      - 'research/beyond-token-time/paper.tex'
      - '.github/workflows/build-beyond-token-time-paper.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y latexmk texlive-latex-base texlive-latex-extra texlive-fonts-recommended
      - name: Build PDF
        working-directory: research/beyond-token-time
        run: latexmk -pdf -interaction=nonstopmode -halt-on-error paper.tex
      - name: Commit generated PDF
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/beyond-token-time/paper.pdf
          if git diff --cached --quiet; then
            echo 'paper.pdf already up to date'
          else
            git commit -m 'Build Beyond Token Time v1.1 PDF'
            git push
          fi
